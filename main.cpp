#include<iostream>

using namespace std;

int main() {
    int n = 5;

    int a[n][n];
    for (int x = 0; x < n; x++) {
        for (int y = 0; y < n; y++) {
            a[x][y] = 0;
        }
    }
    int b[n][n];
    b[n][n] = {0};
    b[n][n];
}
